server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shaileshmandhare/microservices_course_config_store
          search-paths:
           - 'service-config/*service'
          #username: root
          #passphrase: 12345